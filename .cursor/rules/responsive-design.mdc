---
description: 
globs: 
alwaysApply: true
---
Цель: Интерфейс приложения обязан адаптироваться под все размеры экранов — от мобильных до ультрашироких — без потери структуры и UX.

📐 ОБЩИЕ ПРИНЦИПЫ:
- Минимальная ширина: `320px`
- Интерфейс всегда во всю ширину окна (`width: 100vw`)
- Левый сайдбар фиксирован слева, основной контент справа — гибкий и растягивается
- Mobile-first подход: сначала стили для узких экранов, затем адаптация вверх

📦 РАЗМЕТКА:
- Основной layout — **только `flex`**, `grid` допускается **только в крайних случаях**, если `flex` не справляется
- Используются `flex-wrap`, `gap`, `justify-content`, `align-items`, `min-width: 0` и `overflow: hidden` на контейнерах
- Компоненты не имеют фиксированной ширины, за исключением обоснованных случаев (например, сайдбар)

🎨 СТИЛИЗАЦИЯ:
- Приоритет в стилизации:
  1. Props и компоненты `@radix-ui/themes`
  2. Классы Tailwind CSS (если подключён)
  3. Только после этого — SCSS (в формате `.module.scss`)
- Все SCSS-стили:
  - Должны быть модульными (импорт `styles/Button.module.scss`)
  - Размещаются рядом с компонентами
  - Не допускается создание глобальных стилей вне `src/styles/`
  - Общие переменные и миксины — только через `@/styles/variables.scss` и `@use`

🔁 АДАПТАЦИЯ КОМПОНЕНТОВ:
- Кнопки, поля, карточки, блоки — масштабируются без поломки
- Сайдбар скрывается/сворачивается на мобильных (drawer, dropdown)
- Текст не обрезается, адаптивно масштабируется
- Горизонтальный скролл запрещён (`overflow-x: hidden` по умолчанию)

🧠 ПОВЕДЕНИЕ:
- `Sidebar` остаётся слева при любом разрешении
- `Main` блок — адаптивный, заполняет оставшееся пространство
- Точки перелома ориентировочные:
  - 375px — мобильные телефоны
  - 768px — планшеты
  - 1024px и выше — десктоп
- Все компоненты и layout’ы должны быть протестированы в этих размерах

⛔ ЗАПРЕЩЕНО:
- Использовать `grid`, если задачу можно решить через `flex`
- Придумывать кастомные CSS вне библиотек без необходимости
- Использовать inline-стили (`style={{}}`) для layout и визуальных элементов
- Ограничивать ширину контента `max-width: 1440px`
- Использовать `overflow: auto` на `html` или `body` как способ "спрятать" ошибки адаптации

@output Только адаптивные, гибкие компоненты и интерфейсы, соответствующие этим требованиям.